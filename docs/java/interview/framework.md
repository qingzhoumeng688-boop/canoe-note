# 04 框架面试题

> 本篇导读：（待补充）

::: tip 本篇待完善
本专栏目前只有**框架**，正文待补充。每个小节下以 `>` 开头的灰色提示是该节建议写的内容与写法要求，写完后请删掉提示行。
:::

## 一、Spring

> 写作提示：什么是 IOC 与 DI、IOC 容器的两种实现（`BeanFactory` / `ApplicationContext`）、Bean 的作用域、Bean 的生命周期（**完整背一遍**）、循环依赖与三级缓存、注入方式的选择、`@Autowired` 与 `@Resource` 的区别。

（待补充）

## 二、Spring AOP

> 写作提示：AOP 的概念（切面/切点/通知/连接点）、五种通知与执行顺序、JDK 动态代理与 CGLIB 的区别与选择、**为什么同类内部调用 AOP 不生效**、如何自己实现一个简单的 AOP。

（待补充）

## 三、Spring 事务

> 写作提示：`@Transactional` 的实现原理、**事务失效的八大场景**、七种传播行为（**重点讲 REQUIRES_NEW 与 NESTED**）、`rollbackFor` 为什么要配、事务与线程的关系。

（待补充）

## 四、Spring Boot

> 写作提示：自动配置原理（**`@EnableAutoConfiguration` → `AutoConfiguration.imports` → 条件注解**）、起步依赖是什么、如何自定义 starter、`@SpringBootApplication` 由哪三个注解组成、配置文件优先级、多环境配置。

（待补充）

## 五、Spring MVC

> 写作提示：请求处理流程（**DispatcherServlet 全链路**）、`@RequestBody` 与 `@RequestParam` 的区别、拦截器的三个方法、拦截器与过滤器的区别、如何做统一异常处理、如何解决跨域。

（待补充）

## 六、MyBatis

> 写作提示：`#{}` 与 `${}` 的区别与注入风险、一级缓存与二级缓存的作用域与失效场景、Mapper 接口为什么没有实现类、动态 SQL 标签、`@Param` 的作用、分页怎么实现。

（待补充）

## 七、MyBatis-Plus

> 写作提示：条件构造器、分页插件配置、逻辑删除、自动填充、乐观锁插件。

（待补充）

## 八、综合

> 写作提示：Spring 中用到了哪些设计模式（**工厂、单例、代理、模板方法、观察者、策略、适配器、装饰器，逐个指出源码位置**）。

（待补充）

## 本篇小结

> 写作提示：8~12 条 `- ` 开头的要点，每条一句话一个结论，关键词加粗。

（待补充）

## 参考链接

> 写作提示：官方文档优先，5~8 条 `- [标题](url)`。

（待补充）

下一篇 → [05 分布式面试题](/java/interview/distribute)
