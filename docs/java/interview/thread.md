# 02 并发面试题

> 本篇导读：（待补充）

::: tip 本篇待完善
本专栏目前只有**框架**，正文待补充。每个小节下以 `>` 开头的灰色提示是该节建议写的内容与写法要求，写完后请删掉提示行。
:::

## 一、线程基础

> 写作提示：线程与进程的区别、创建线程的几种方式、`start()` 与 `run()` 的区别、线程的生命周期与状态流转、如何优雅停止线程、守护线程是什么。

（待补充）

## 二、synchronized

> 写作提示：作用范围与锁的对象、对象头 Mark Word、锁升级过程、可重入性、与 `Lock` 的区别、底层 `monitorenter` 指令、死锁的四个条件与排查。

（待补充）

## 三、volatile

> 写作提示：三大特性（可见性、有序性、不保证原子性）、JMM 与主内存/工作内存、happens-before 规则、DCL 单例为什么必须加 volatile、内存屏障。

（待补充）

## 四、ThreadLocal

> 写作提示：实现原理（ThreadLocalMap、弱引用 key）、内存泄漏的原因与规避、父子线程传值、在线程池中使用的问题、典型应用场景。

（待补充）

## 五、线程池

> 写作提示：七大参数与执行流程、为什么队列满了才创建非核心线程、四种拒绝策略、Executors 的坑、submit 与 execute 的区别、线程数怎么定、怎么监控。

（待补充）

## 六、CAS 与 AQS

> 写作提示：CAS 的原理与三个问题（ABA、自旋开销、只能一个变量）、AtomicInteger 与 LongAdder 的区别、AQS 的三大组件与两种模式、基于 AQS 实现了哪些类。

（待补充）

## 七、并发容器

> 写作提示：ConcurrentHashMap 在 JDK 7 与 JDK 8 的区别、为什么不允许 null、CopyOnWriteArrayList 的适用场景、BlockingQueue 家族、ConcurrentLinkedQueue。

（待补充）

## 八、并发工具

> 写作提示：CountDownLatch 与 CyclicBarrier 的区别、Semaphore 的使用场景、CompletableFuture 的常用方法与"多任务并行聚合"写法。

（待补充）

## 九、综合题

> 写作提示：三个线程按顺序打印 ABC、生产者消费者、交替打印奇偶、单例的线程安全写法、实现一个阻塞队列（**给出代码**）。

（待补充）

## 十、排查类

> 写作提示：CPU 飙高怎么排查、死锁怎么排查、线程数暴涨怎么排查、`jstack` 输出怎么看。

（待补充）

## 本篇小结

> 写作提示：8~12 条 `- ` 开头的要点，每条一句话一个结论，关键词加粗。

（待补充）

## 参考链接

> 写作提示：官方文档优先，5~8 条 `- [标题](url)`。

（待补充）

下一篇 → [03 JVM 面试题](/java/interview/jvm)
