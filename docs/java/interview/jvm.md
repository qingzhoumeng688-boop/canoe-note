# 03 JVM 面试题

> 本篇导读：（待补充）

::: tip 本篇待完善
本专栏目前只有**框架**，正文待补充。每个小节下以 `>` 开头的灰色提示是该节建议写的内容与写法要求，写完后请删掉提示行。
:::

## 一、JVM 结构

> 写作提示：运行时数据区有哪几块、哪些线程共享哪些私有、程序计数器为什么没有 OOM、栈帧的结构、堆的分代结构。

（待补充）

## 二、类加载

> 写作提示：类加载的七个阶段、什么时候触发初始化、双亲委派模型与为什么这么设计、什么场景下会破坏双亲委派（**JDBC、Tomcat**）、如何自定义类加载器。

（待补充）

## 三、对象

> 写作提示：对象的创建过程、内存布局（对象头/实例数据/对齐填充）、对象头 Mark Word 里有什么、对象的访问定位方式、对象什么时候会进入老年代。

（待补充）

## 四、内存分配与回收

> 写作提示：Minor GC / Major GC / Full GC 的区别与触发条件、判断对象存活的两种方法、四种引用类型、GC Roots 有哪些、空间分配担保。

（待补充）

## 五、GC 算法

> 写作提示：标记-清除/复制/标记-整理的优缺点与适用场景、分代收集理论的三个假说、为什么新生代用复制算法、跨代引用问题与卡表。

（待补充）

## 六、垃圾收集器

> 写作提示：CMS 的四个阶段与三个缺点、G1 的 Region 与可预测停顿模型、为什么 G1 取代 CMS、ZGC 的着色指针与读屏障、怎么选择收集器。

（待补充）

## 七、内存模型

> 写作提示：JMM 是什么、主内存与工作内存、happens-before 的 8 条规则、volatile / synchronized / final 的内存语义、指令重排序与 as-if-serial。

（待补充）

## 八、调优与排障

> 写作提示：常用 JVM 参数、怎么开启和分析 GC 日志、OOM 的几种类型与排查流程（**jmap + MAT**）、CPU 飙高的排查命令链、`jstat -gcutil` 怎么看。

（待补充）

## 九、实战题

> 写作提示：线上频繁 Full GC 怎么排查、元空间 OOM 的常见原因、堆外内存泄漏怎么查、一个服务该给多大堆内存。

（待补充）

## 本篇小结

> 写作提示：8~12 条 `- ` 开头的要点，每条一句话一个结论，关键词加粗。

（待补充）

## 参考链接

> 写作提示：官方文档优先，5~8 条 `- [标题](url)`。

（待补充）

下一篇 → [04 框架面试题](/java/interview/framework)
